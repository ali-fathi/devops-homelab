apiVersion: v1
kind: Service
metadata:
  name: grafana-lb
  namespace: monitoring
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.178.212

  selector:
    app.kubernetes.io/name: grafana

  ports:
    - port: 80
      targetPort: 3000
