deploymentMode: SingleBinary

loki:
  auth_enabled: false

  commonConfig:
    replication_factor: 1

  storage:
    type: filesystem

    # Required by recent Grafana Loki Helm chart versions,
    # even when running SingleBinary mode with filesystem storage.
    bucketNames:
      chunks: loki-chunks
      ruler: loki-ruler
      admin: loki-admin

  schemaConfig:
    configs:
      - from: "2024-01-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: loki_index_
          period: 24h

  limits_config:
    # 30 days retention
    retention_period: 720h

    # Reject old logs older than 7 days at ingestion time
    reject_old_samples: true
    reject_old_samples_max_age: 168h

  compactor:
    retention_enabled: true
    delete_request_store: filesystem

  # Loki Ruler configuration for log-based alerting.
  # This sends Loki/LogQL alerts to the existing Alertmanager instance.
  rulerConfig:
    enable_api: true
    enable_alertmanager_v2: true

    alertmanager_url: http://monitoring-kube-prometheus-alertmanager.monitoring.svc.cluster.local:9093

    evaluation_interval: 1m

    storage:
      type: local
      local:
        directory: /var/loki/rules

    rule_path: /tmp/loki-rules

    ring:
      kvstore:
        store: inmemory

singleBinary:
  replicas: 1

  persistence:
    enabled: true
    storageClass: longhorn
    size: 20Gi

  # Mount Loki alert rules from ConfigMap.
  # The folder "fake" is used because auth_enabled=false.
  extraVolumes:
    - name: loki-alert-rules
      configMap:
        name: loki-log-alert-rules

  extraVolumeMounts:
    - name: loki-alert-rules
      mountPath: /var/loki/rules/fake
      readOnly: true

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi

gateway:
  enabled: true

read:
  replicas: 0

write:
  replicas: 0

backend:
  replicas: 0

# Disable cache components for the homelab setup.
# This avoids unnecessary memcached service lookups and keeps the stack simpler.
chunksCache:
  enabled: false

resultsCache:
  enabled: false

monitoring:
  serviceMonitor:
    enabled: true

test:
  enabled: false