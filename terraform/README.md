# Terraform

This directory contains Infrastructure as Code.

Current usage:

- Kubernetes resources
- Future cloud resources
- Reusable modules

Workflow:

```bash
terraform init
terraform plan
terraform apply
```
